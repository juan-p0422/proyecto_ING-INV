DATABASE_URL=postgresql://eduroom:cambia_esta_clave@localhost:5432/eduroom?schema=public
JWT_SECRET=cambia_este_secreto_por_uno_de_al_menos_32_caracteres
JWT_EXPIRES_IN=8h
STRICT_INTEGRITY=false
# Opcional: ruta absoluta o relativa al manifest. Si se omite, se busca en la raíz.
INTEGRITY_MANIFEST_PATH=
PORT=3000
# Uno o varios orígenes exactos separados por comas.
CORS_ORIGIN=http://localhost:5173
